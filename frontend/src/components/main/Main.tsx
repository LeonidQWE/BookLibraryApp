import { BookForm, Container } from 'components';

export const Main = () => {
  return (
    <main>
      <Container>
        <div>
          <BookForm />
        </div>
      </Container>
    </main>
  );
};
